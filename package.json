{
  "packageManager": "yarn@3.3.1",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "web": "yarn workspace @raft/web",
    "worker": "yarn workspace @raft/worker",
    "typecheck": "yarn workspaces foreach run typecheck"
  }
}
