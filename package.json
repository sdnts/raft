{
  "packageManager": "yarn@3.3.1",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "common": "yarn workspace @raft/common",
    "api": "yarn workspace @raft/api",
    "ui": "yarn workspace @raft/ui",
    "typecheck": "yarn workspaces foreach run typecheck"
  }
}
